{{/* Input to create instance in the simulator */}}
{{/* =% instanceProvision `cloud1/net` %= */}}

Tags:
  cloud: cloud1
  type: network
  name: cloud1-net1
Properties:
  cidr: 10.20.100.0/24
  gateway: 10.20.0.1
